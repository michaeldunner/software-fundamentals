#include <iostream>

// pragma once is a preprocessor directive that ensures the file is included
// only once in a single compilation, preventing multiple definitions and
// potential errors. It is a modern alternative to traditional include guards.

#pragma once

int main() {

    // there isn't a slideshow for today?

}
