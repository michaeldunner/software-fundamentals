#include <Time.h>

#include <iostream>

int main() {
    // destructors are defined by a ~ before the class name, and they are called
    // automatically when an object goes it has no parameters and no return
    // type, and it is used to clean up resources when an object goes out of
    // scope
    // destructors must be public

    // use delete to call the destructor
}
